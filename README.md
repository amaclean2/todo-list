`Tech Stack`
db:
	mongodb

server:
	testing:
		mocha
		chai
	frameworks:
		mongoose
		express

client:
	testing:
		jest
		enzyme
	data:
		redux
	components:
		react
	styling:
		Sass

`Start Up`
docker-compose up build

`Tear Down`
docker-compose down
